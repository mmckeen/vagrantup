#ifndef VAGRANTFILE_H
#define VAGRANTFILE_H

#include <QObject>

class Vagrantfile : public QObject
{
    Q_OBJECT
public:
    explicit Vagrantfile(QObject *parent = 0);

signals:

public slots:

};

#endif // VAGRANTFILE_H
